<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - {{site.description}} ({{site.name}})</title>
    <link rel="stylesheet" href="{{ '/assets/css/style.css' | url }}">
</head>

<body>
    {% include 'landmarks/header' %}
    <main>
        <h1>{{title}}</h1>
        <h3>{{description}}</h3>
        {{content}}
    </main>
</body>

</html>